replicaCount: 1

image:
  repository: yourrepo/agent-backend
  tag: latest
  pullPolicy: IfNotPresent

serviceAccount:
  create: true
  name: agent-backend-sa

env:
  OAUTH_ISSUER: "https://accounts.google.com"
  OAUTH_CLIENT_ID: "YOUR_CLIENT_ID"
  REQUIRED_ROLE: "user"
  RABBITMQ_HOST: "mq"
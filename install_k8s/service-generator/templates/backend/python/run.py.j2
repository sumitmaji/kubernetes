#!/usr/bin/env python3
"""
{{ service_name }} - Main Application Entry Point
Generated by Service Template Generator

This file serves as the entry point for the Flask application.
It initializes the app and runs it with the configured settings.
"""

from app import create_app
import os

# Create Flask application instance
app = create_app()

if __name__ == '__main__':
    # Development server configuration
    host = os.getenv('HOST', '0.0.0.0')
    port = int(os.getenv('PORT', {{ backend_port }}))
    debug = os.getenv('FLASK_ENV') == 'development'
    
    print(f"Starting {{ service_name }} server...")
    print(f"Host: {host}")
    print(f"Port: {port}")
    print(f"Debug: {debug}")
    
    # Run the application
    app.run(
        host=host,
        port=port,
        debug=debug,
        threaded=True
    )
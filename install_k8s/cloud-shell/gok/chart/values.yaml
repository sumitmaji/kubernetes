replicaCount: 1

image:
  repository: registry.gokcloud.com/cloudshell
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  host: cloudshell.gokcloud.com
  annotations: {}
resources: {}